export const app = {
    name: 'Infinity Cast',
    version: '1.0.0',
    repository: 'https://github.com/EpiMac/infinity-cast'
};

export function title(page) {
    return `${app.name} — ${page}`;
}
